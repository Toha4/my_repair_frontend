import React from "react";

interface ITableExpensesCheck {
  search?: string;
  filter?: any;
}

const TableExpensesCheck: React.FC<ITableExpensesCheck> = ({ search, filter }) => {

  return (
    <React.Fragment>
      TableExpensesCheck
    </React.Fragment>
  )
}

export default TableExpensesCheck;